DATABASE_URL = "postgresql+psycopg://postgres:postgres@localhost:7432/postgres"
REDIS_URL = "redis://localhost:7379/0"
PAYMENT_API_URL = "http://localhost:9001"
PROTECTION_API_URL = "http://localhost:9002"
BOOKING_TTL_MINUTES = 15


# --- Лок на загрузку мероприятия (SingleFlight) ------------------------------

# Через сколько лок протухнет сам, если державший его процесс умер.
REDIS_EVENT_LOCK_TTL_SECONDS = 3
# Сколько ждущий простоит за локом, прежде чем получить 503.
REDIS_EVENT_LOCK_WAIT_SECONDS = 1


# --- Кэш мероприятия ---------------------------------------------------------

REDIS_EVENT_CACHE_TTL_SECONDS = 60
# Отдельный короткий TTL для «мероприятия нет в БД»: чем он меньше, тем
# быстрее в выдаче появится только что созданное мероприятие.
REDIS_EVENT_NOT_FOUND_TTL_SECONDS = 10
# Доля от TTL, на которую он случайно растягивается, чтобы ключи не протухали разом.
REDIS_EVENT_CACHE_TTL_JITTER_RATIO = 0.2


# --- Дедупликация просмотров -------------------------------------------------

# Окно, в течение которого повторный просмотр мероприятия с того же IP
# не засчитывается.
REDIS_EVENT_VIEW_DEDUPE_TTL_SECONDS = 5 * 60


# --- Агрегация просмотров перед записью в БД ---------------------------------

# Потолок очереди: при переполнении просмотры теряются, но запрос не ждет.
VIEWS_QUEUE_MAXSIZE = 10_000
# Сбрасываем в БД либо по накоплению стольких просмотров...
VIEWS_FLUSH_BATCH_SIZE = 10
# ...либо по истечении этого времени — что наступит раньше.
VIEWS_FLUSH_INTERVAL_SECONDS = 5
# Сколько ждем воркер при остановке приложения, прежде чем снять его принудительно.
VIEWS_SHUTDOWN_TIMEOUT_SECONDS = 10
