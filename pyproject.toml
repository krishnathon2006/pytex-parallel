[project]
name = "pytex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "httpx>=0.28.1",
]

[tool.fastapi]
entrypoint = "weeks.week_01.main:app"
